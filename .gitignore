# Virtual environment
venv/
.venv/
env/

# Python cache
__pycache__/
*.pyc
*.pyo
*.pyd
.Python
*.so

# ── Data files ────────────────────────────────────────────
# Raw data — large, download/generate separately
data/raw/*.csv
data/raw/*.graphml
data/raw/*.tif
data/raw/srtm_cache/

# Keep processed graph and trained model — needed for Streamlit deployment
# data/processed/*.graphml  ← KEEP
# data/processed/*.csv      ← KEEP (benchmark results)
# models/*.pkl              ← KEEP

# ── Utility/scratch scripts (not part of core project) ────
check_kaggle.py
run_benchmark_50.py
sanity_check.py
fix_elevation.py
demo.py

# ── IDE settings ──────────────────────────────────────────
.idea/
.vscode/
*.swp
*.swo
*~

# ── OS files ──────────────────────────────────────────────
.DS_Store
Thumbs.db
desktop.ini

# ── Jupyter ───────────────────────────────────────────────
.ipynb_checkpoints/
*.ipynb

# ── Cache directories ─────────────────────────────────────
cache/
.cache/
.streamlit/

# ── Coverage & testing artifacts ──────────────────────────
htmlcov/
.coverage
.coverage.*
coverage.xml
*.cover
.pytest_cache/

# ── Build artifacts ───────────────────────────────────────
build/
dist/
*.egg-info/
*.egg

# ── Logs & env ────────────────────────────────────────────
*.log
.env
.env.local